# see https://perlhacks.com/2024/01/github-actions-for-perl-development/

---
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    uses: dod38fr/github_workflows/.github/workflows/cpan-dzil-test.yml@dzil
    with:
      os: '["ubuntu-latest"]'
      listdeps_args: '--no-recommends --no-suggests'
